package com.whatpl.global.pagination;

/**
 * SliceResponse<T extends SliceElement> 객체, SliceUtils 객체의 hasNext 메서드에 사용될 마커 인터페이스
 * Slice.content 필드의 element에 해당하는 객체에 SliceElement 타입을 마킹한다.
 */
public interface SliceElement {
}
